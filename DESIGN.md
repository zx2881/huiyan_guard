# Design System

## Direction

宿舍管理员在白天办公室或走廊中，用手机快速核验一份 AI 巡检报告。界面采用浅色、克制的产品工具风格，信息密度适中，强调证据、状态和操作顺序。

## Color

使用 OKLCH 色值。中性色轻微偏青，主色为低饱和青绿色，仅用于主操作、焦点和关键状态。成功、警告、错误分别使用绿、琥珀和红色浅底，并始终配文字标签。

核心变量定义在 `frontend/assets/css/common.css`：`--canvas`、`--surface`、`--ink`、`--muted`、`--line`、`--accent` 及语义状态色。

## Typography

使用系统无衬线字体栈，中文优先 Microsoft YaHei。标题依靠字号与字重建立层级；正文最大行长 72ch。产品界面不使用展示字体。

## Layout

桌面最大内容宽度 70rem，主要断点为 760px 和 480px。上传页采用主任务与流程说明双栏，报告采用照片与总结双栏；窄屏全部重排为单栏。打印时使用 15mm 页边距并隐藏交互控件。

## Components

- 顶部导航：品牌、当前页面和主要页面入口。
- 按钮：主按钮、次按钮和危险按钮三种，使用同一尺寸与圆角。
- 状态标签：文字与浅色底同时表达状态。
- 隐患条目：单层完整边框，内部按证据、判断、建议、依据和复核操作组织。
- 表单：标准标签、输入框和文本域，修正及补录在页面内展开。
- 反馈：加载、空结果、警告和错误都有独立文本状态。

## Interaction

状态变化使用 180ms 的缓出过渡，不使用装饰动画。复核操作保存后刷新当前报告；执行中报告按后台阶段轮询。所有模型和用户文本通过 DOM 文本节点渲染。
