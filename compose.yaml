services:
  app:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      REQUIRE_ACCESS_CONTROL: "true"
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    init: true
