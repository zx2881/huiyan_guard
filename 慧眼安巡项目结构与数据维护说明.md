# 慧眼安巡项目结构与数据维护说明

## 1. 项目是什么

慧眼安巡是一套“拍照后直接给出安全整改建议”的校园安全巡检系统。用户用手机或电脑选择检查场景，拍摄或上传一张现场照片，系统按照场景检查清单分析图片，识别可能存在的安全隐患，匹配相关规章条款，判断风险，并直接输出整改建议。

本版本的核心目标是把一次现场照片快速变成一份有依据的分析结果。系统会保存本次巡检记录，方便之后查看历史报告和统计隐患情况。

当前流程不包含提交整改结果、上传整改照片、现场复查或销项。用户看到整改建议后，本次巡检流程结束。AI 内部仍会进行一次结论复核，但这是生成建议时的质量检查，不代表现场问题已经整改完成。

## 2. 一次巡检怎么工作

```text
选择检查场景
    ↓
拍照或上传现场照片
    ↓
保存本次巡检记录和现场照片
    ↓
AI 识别照片中的隐患
    ↓
检索相关安全规章
    ↓
判断风险等级
    ↓
生成整改建议
    ↓
AI 复核分析结果
    ↓
保存并展示分析报告
```

结果页面主要展示：

- 现场照片和检查场景；
- 是否发现明确隐患；
- 隐患名称、位置和证据说明；
- 风险等级或风险提示；
- 相关规章条款和出处；
- 具体整改建议；
- 本次分析的时间和处理状态。

如果照片不清晰、角度不足或无法确认某个问题，系统应明确说明“无法从当前照片确认”，不能把不确定内容说成确定事实。未发现明确隐患也不等于整个场所绝对安全。

## 3. 项目由哪些部分组成

可以把整个项目理解成五部分：

| 部分 | 通俗解释 | 主要职责 |
|---|---|---|
| 前端网页 | 用户操作的窗口 | 选择场景、上传照片、查看进度和结果、查询历史报告 |
| 后端程序 | 负责组织工作的调度员 | 接收请求、保存照片、调用 AI、保存结果、提供统计数据 |
| AI 分析模块 | 分工合作的分析人员 | 看图、识别隐患、判断风险、给出建议、复核结论 |
| 检查清单与规章知识库 | 检查表和参考手册 | 规定查什么、依据什么判断 |
| 数据和文件存储 | 系统档案室 | 保存照片、巡检记录、分析结果和规章资料 |

用户上传照片后，前端将照片和场景发送给后端；后端保存照片并创建一条巡检记录；AI 按检查清单识别隐患；系统从规章知识库中找到相关条款；分析结果写入数据库，再由前端展示。

## 4. 建议的代码目录

```text
慧眼安巡/
├─ README.md                         # 项目介绍、启动和部署说明
├─ .gitignore                        # 排除密钥、数据库、照片和日志
├─ .env.example                      # 模型 API 等环境变量模板
├─ requirements.txt                  # Python 依赖
├─ Dockerfile                        # 应用镜像
├─ docker-compose.yml                # 部署配置
│
├─ backend/
│  └─ app/
│     ├─ __init__.py
│     ├─ main.py                      # FastAPI 入口和静态页面托管
│     ├─ config.py                    # 模型、数据库、存储路径配置
│     ├─ database.py                  # SQLite 连接和初始化
│     │
│     ├─ api/                         # HTTP 接口层
│     │  ├─ __init__.py
│     │  ├─ inspections.py            # 创建巡检、上传照片、查询结果、分析进度
│     │  ├─ reports.py                # 历史报告和单次报告数据
│     │  ├─ dashboard.py              # 统计看板数据
│     │  └─ knowledge.py              # 规章条款管理和索引更新
│     │
│     ├─ schemas/                     # Pydantic 数据格式
│     │  ├─ __init__.py
│     │  ├─ inspection.py             # 巡检请求、结果和进度事件
│     │  ├─ hazard.py                 # 隐患、风险、整改建议和引用依据
│     │  ├─ knowledge.py              # 规章条款和来源
│     │  └─ workflow.py               # AI 工作流状态和智能体输入输出
│     │
│     ├─ models/                      # SQLite 数据模型
│     │  ├─ __init__.py
│     │  ├─ inspection.py             # 巡检记录和现场照片
│     │  ├─ hazard.py                 # 隐患分析结果和规章引用快照
│     │  └─ knowledge.py              # 规章原文、来源和适用场景
│     │
│     ├─ services/                    # 业务处理层
│     │  ├─ __init__.py
│     │  ├─ inspection_service.py     # 创建巡检、启动分析、保存结果
│     │  ├─ report_service.py         # 汇总巡检数据并生成报告
│     │  ├─ dashboard_service.py      # 统计巡检量、隐患类型和风险等级
│     │  └─ knowledge_service.py      # 条款维护和向量索引同步
│     │
│     ├─ agents/                      # AI 分析步骤
│     │  ├─ __init__.py
│     │  ├─ perceive.py               # 视觉感知：按清单识别结构化隐患
│     │  ├─ classify.py               # 风险判断：结合规章分析等级
│     │  ├─ remediate.py              # 生成具体整改建议
│     │  ├─ review.py                 # 复核识别、定级和建议是否合理
│     │  └─ prompts/                  # 各步骤的提示词
│     │     ├─ perceive.md
│     │     ├─ classify.md
│     │     ├─ remediate.md
│     │     └─ review.md
│     │
│     ├─ workflow/                    # AI 流程编排
│     │  ├─ __init__.py
│     │  ├─ state.py                  # 分析状态和允许的状态转换
│     │  ├─ runner.py                 # 感知→分级→建议→复核→定稿
│     │  └─ events.py                 # 向前端发布分析进度
│     │
│     ├─ rag/                         # 规章检索增强
│     │  ├─ __init__.py
│     │  ├─ embeddings.py              # 中文向量模型封装
│     │  ├─ indexer.py                # 条款切分、向量化、更新 Chroma
│     │  └─ retriever.py              # 按场景和隐患描述检索条款
│     │
│     ├─ integrations/                # 外部模型 API 封装
│     │  ├─ __init__.py
│     │  ├─ vision_client.py          # 视觉模型调用
│     │  └─ text_client.py            # 文本推理模型调用
│     │
│     └─ utils/
│        ├─ __init__.py
│        ├─ file_storage.py            # 现场照片保存和路径处理
│        └─ logging.py                 # 日志配置
│
├─ frontend/
│  ├─ index.html                     # 智能巡检：上传、进度、结果
│  ├─ records.html                   # 历史巡检记录和报告入口
│  ├─ report.html                    # 单次报告详情和打印
│  ├─ dashboard.html                 # 隐患和风险统计看板
│  ├─ admin/
│  │  └─ knowledge.html              # 规章知识库管理
│  └─ assets/
│     ├─ css/
│     │  ├─ common.css
│     │  ├─ inspection.css
│     │  ├─ records.css
│     │  ├─ report.css
│     │  └─ dashboard.css
│     ├─ js/
│     │  ├─ api.js                   # 统一请求和分析进度连接
│     │  ├─ common.js
│     │  ├─ inspection.js             # 上传、进度和结果展示
│     │  ├─ records.js                # 历史记录查询
│     │  ├─ report.js                 # 报告展示和打印
│     │  ├─ dashboard.js              # ECharts 图表
│     │  └─ knowledge.js              # 条款维护
│     └─ vendor/
│        └─ echarts.min.js
│
├─ knowledge/                        # 随项目维护的初始配置
│  ├─ sources/
│  │  └─ README.md                   # 规章来源和获取日期
│  ├─ dormitory/
│  │  ├─ checklist.json              # 宿舍检查项目
│  │  └─ regulations.json            # 宿舍规章条款
│  └─ laboratory/
│     ├─ checklist.json              # 实验室检查项目
│     └─ regulations.json            # 实验室规章条款
│
├─ data/                             # 运行时数据，不提交到 Git
│  ├─ app.db                         # SQLite 业务数据库
│  ├─ chroma/                        # Chroma 向量索引
│  ├─ uploads/
│  │  └─ inspections/                # 首次巡检现场照片
│  └─ logs/                          # 运行日志
│
├─ scripts/
│  ├─ init_db.py                     # 初始化数据库
│  ├─ import_knowledge.py            # 导入规章并建立索引
│  ├─ seed_demo.py                   # 写入演示巡检数据
│  └─ evaluate.py                    # 评估识别结果和耗时
│
├─ tests/
│  ├─ conftest.py
│  ├─ test_workflow.py               # 工作流、复核和失败处理
│  ├─ test_rag.py                    # 条款检索和来源引用
│  ├─ test_inspection_api.py         # 上传、创建、查询巡检
│  └─ fixtures/
│     ├─ images/
│     └─ annotations.json
│
├─ deploy/
│  └─ nginx.conf                     # 反向代理和上传配置
│
└─ docs/
   ├─ architecture.md               # 架构和状态说明
   ├─ api.md                         # 接口约定
   ├─ data-model.md                  # 数据结构和关联关系
   ├─ evaluation.md                  # 测试和评估口径
   └─ demo-script.md                 # 演示脚本
```

## 5. 代码之间怎样配合

```text
frontend/assets/js/inspection.js
        ↓ HTTP 请求 / 进度事件
api/inspections.py
        ↓
services/inspection_service.py
        ↓
workflow/runner.py
        ├─ agents/perceive.py   → vision_client.py
        ├─ agents/classify.py   → retriever.py + text_client.py
        ├─ agents/remediate.py → retriever.py + text_client.py
        └─ agents/review.py    → text_client.py
        ↓
保存巡检、隐患、建议和规章引用
        ↓
前端展示报告，用户查看后结束本次巡检
```

各层职责要保持清楚：

- `api/` 只处理请求、参数和响应，不把复杂 AI 逻辑写在路由里。
- `services/` 负责业务流程和数据库读写。
- `agents/` 负责 AI 分析，不直接处理网页。
- `workflow/` 负责安排 AI 分析顺序和失败重试。
- `schemas/` 约束接口和模型输出格式。
- `models/` 描述数据库如何保存数据。
- `rag/` 只负责规章条款检索和索引。
- `integrations/` 隔离外部模型供应商，方便换模型。

## 6. 三类数据要分开

### 6.1 场景检查清单：告诉系统“查什么”

例如宿舍检查清单可以包含：插线板是否被覆盖、通道是否堵塞、违规电器是否出现等。它不负责保存某次巡检结果。

示例结构：

```json
{
  "scene": "dormitory",
  "items": [
    {
      "id": "socket_cover",
      "name": "插线板是否被物品覆盖",
      "instruction": "检查插线板周围及表面是否有衣物等覆盖物"
    }
  ]
}
```

新增检查项目时，修改对应场景的 `checklist.json`，同时准备测试图片，并检查模型是否能根据照片作出可靠判断。

### 6.2 规章知识库：告诉系统“依据什么判断”

每条规章建议保存：稳定编号、适用场景、文件名称、条款编号、原文和来源链接。

```json
{
  "id": "dorm_rule_001",
  "scene": "dormitory",
  "document_title": "实际使用的规章文件名称",
  "article": "实际条款编号",
  "content": "经过核验的规章原文",
  "source_url": "原文来源链接"
}
```

上面的文字只是格式示例，正式录入时必须替换为核验过的真实规章内容。每次分析时，系统根据“隐患类型 + 描述”检索相关条款，并把条款内容和出处写入本次结果。

### 6.3 巡检业务记录：告诉系统“这次发生了什么”

每次巡检至少保存：

- 巡检编号；
- 检查场景；
- 现场照片路径；
- 创建时间和完成时间；
- 分析状态；
- 隐患名称、位置、证据说明；
- 风险等级或风险提示；
- 整改建议；
- 当时引用的规章原文、条款编号和出处；
- AI 复核结果和错误信息（如有）。

这些数据由用户操作和系统运行产生，写入 SQLite，不通过手工修改 JSON 维护。

## 7. SQLite、Chroma 和照片分别保存什么

| 存储位置 | 保存内容 | 作用 |
|---|---|---|
| SQLite | 巡检记录、隐患、建议、规章原文和引用快照 | 正式业务记录，支持查询和统计 |
| Chroma | 规章条款的向量和元数据 | 根据隐患含义快速找相关条款 |
| `data/uploads/inspections/` | 用户首次上传的现场照片 | 保存现场证据 |

照片不建议直接塞进 SQLite。数据库保存照片路径、文件名、上传时间和所属巡检编号即可。

`knowledge/` 中的 JSON 是项目自带的初始资料；导入后，运行中的规章以 SQLite 为正式数据源，Chroma 是根据规章建立的检索索引。修改条款后必须同步更新索引。

## 8. 怎样写入初始知识和规章

### 8.1 开发阶段批量导入

1. 收集真实的宿舍、实验室安全规章。
2. 整理条款编号、原文、来源和适用场景。
3. 写入对应场景的 `regulations.json`。
4. 运行 `scripts/import_knowledge.py`，导入 SQLite。
5. 对条款生成向量，写入 Chroma。
6. 用已知隐患描述测试是否检索到正确条款。

导入程序应使用稳定的条款编号。再次导入同一个编号时更新原记录，不要重复插入。

### 8.2 系统运行后通过后台维护

知识库后台的写入流程为：

```text
新增或修改规章条款
    ↓
保存规章原文到 SQLite
    ↓
更新该条款的 Chroma 索引
    ↓
返回索引更新成功或失败
```

如果索引更新失败，后台必须明确提示并允许重试，不能让用户以为新规章已经参与分析。

修改规章后，新巡检使用新内容；旧报告保留当时引用的条款快照，避免历史报告随着规章修改而改变。

## 9. 一次巡检怎样写入数据库

| 步骤 | 系统动作 | 写入内容 |
|---|---|---|
| 1 | 用户选择场景并上传照片 | 巡检记录、场景、照片路径、创建时间 |
| 2 | 开始 AI 分析 | 分析中状态 |
| 3 | 完成视觉识别 | 隐患候选、位置、证据说明 |
| 4 | 完成规章检索和风险判断 | 风险等级、条款引用快照 |
| 5 | 生成和复核整改建议 | 整改建议、复核结果 |
| 6 | 分析成功或失败 | 完成状态、错误信息、完成时间 |
| 7 | 前端展示并保存报告 | 可查询的完整巡检结果 |

一条记录的逻辑关系如下：

```text
一次巡检
  ├─ 一张或多张现场照片
  └─ 一个分析结果
      ├─ 一个或多个隐患
      │   ├─ 风险提示
      │   ├─ 证据说明
      │   ├─ 规章引用
      │   └─ 整改建议
      └─ 分析状态和错误信息
```

当前版本不保存整改任务、整改提交、整改照片、复查记录或销项状态。

## 10. 后续修改应该改哪里

| 想做的修改 | 主要位置 | 需要同步检查 |
|---|---|---|
| 修改颜色、字体和布局 | `frontend/assets/css/` | 通常不涉及后端 |
| 修改上传和结果展示 | `frontend/assets/js/inspection.js` | 请求或字段变化时同步接口 |
| 增加宿舍检查项目 | `knowledge/dormitory/checklist.json` | 规章条款和测试图片 |
| 新增或修改规章 | 知识库后台或 `regulations.json` | SQLite 和 Chroma 索引 |
| 调整 AI 提示语 | `agents/prompts/` | 用测试图片重新验证 |
| 更换模型供应商 | `integrations/` 和环境变量 | 输出格式、超时和错误处理 |
| 调整 AI 执行顺序 | `workflow/runner.py` | 状态展示和工作流测试 |
| 增加结果字段 | `schemas/`、`models/`、服务层和前端 | 数据库结构和历史数据兼容 |
| 增加看板统计 | `dashboard_service.py` 和 `dashboard.js` | 现有记录是否包含所需字段 |
| 修改报告内容 | `report_service.py`、`report.html`、报告样式 | 数据是否已保存 |
| 增加新场景 | 新场景清单、规章和场景选择入口 | 模型输出和流程是否适用 |

增加数据库字段时，不能只修改 Python 模型。已有数据库需要通过迁移增加字段，并先备份数据，不能为了加一个字段直接删库重建。

## 11. 新增一个场景要做什么

以“消防通道巡检”为例：

1. 创建该场景的检查清单；
2. 整理并核验对应的安全规章；
3. 导入条款并建立向量索引；
4. 在前端场景选择中加入该场景；
5. 准备现场图片，测试隐患识别、风险提示、条款引用和整改建议；
6. 检查报告和看板是否能正确显示新场景。

只要新场景仍然是“上传照片—分析隐患—给出建议”，就可以复用大部分代码。如果某个场景需要额外的人工填写信息，也要同步增加表单、接口和数据字段。

## 12. 建议的开发顺序

1. 做上传页面、后端接收和照片保存；
2. 接通视觉模型，返回结构化隐患；
3. 导入规章并完成检索；
4. 加入风险判断、整改建议和 AI 复核；
5. 保存完整巡检记录并生成报告；
6. 增加历史记录和统计看板；
7. 增加实验室场景并完成公网部署。

每一步都要验证：结果是否保存、条款是否引用正确、模型失败时是否有明确状态、历史报告是否能够再次打开。

## 13. 当前尚待确定的事项

以下内容在方案中还没有完全确定，后续开发时需要由团队定稿：

- 风险等级具体分为几级，以及每一级的定义；
- 没有检索到适用规章时的展示方式；
- 是否允许用户在结果页人工修改隐患和建议；
- 照片最大大小、格式和保存期限；
- 是否需要登录，以及最小权限规则；
- 统计看板的时间范围和筛选条件；
- 真实模型供应商、模型版本和调用费用控制；
- 数据库升级和历史数据备份方案。

这些属于产品和工程决策，不能仅靠模型提示词自动决定。

## 14. 当前版本的范围边界

### 纳入

- 宿舍场景的拍照巡检；
- 实验室作为第二演示场景；
- 图片隐患识别；
- 风险提示和规章引用；
- 直接输出整改建议；
- 历史巡检记录和报告；
- 隐患类型、风险等级和巡检数量统计；
- 规章知识库管理。

### 暂不纳入

- 提交整改结果；
- 上传整改照片；
- 整改复查和销项；
- 整改完成率统计；
- 实时视频连续巡检；
- 原生 App 和小程序；
- 专用传感器和固定摄像头；
- 复杂组织权限和消息推送。

---

本文档描述的是当前简化后的产品和代码设想。原始参赛方案仍保留在项目根目录的 Word 文件中；如果两者有冲突，以本说明中“拍照后直接输出整改建议、不提交整改照片、不复查”的当前范围为准。
