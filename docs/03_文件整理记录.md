# 文件整理记录

整理日期：2026-09-16。根据用户要求实际移动、合并和删除文件，并将 9/28 前基本完成作为项目硬目标。

## 文件夹整理完成时的目录快照

```text
慧眼巡安/
├─ README.md                       总入口
├─ huiyan_guard/                   代码仓库，保留 .git
│  ├─ README.md                    仅说明现有代码与启动方法
│  ├─ backend/                     后端业务代码
│  ├─ frontend/                    三个页面及公共样式
│  ├─ knowledge/                   场景清单与规章来源说明
│  ├─ samples/测试.jpg             同事提供的样图
│  └─ 配置与依赖文件
├─ 项目文档/
│  ├─ 01_项目实施方案.md            唯一执行计划，含八步实施与每日验收
│  ├─ 02_当前进展.md                已完成/待完成与核验边界
│  ├─ 03_文件整理记录.md            本文件
│  ├─ 原始产品方案.docx             保留一份原始立项方案
│  └─ 核验附件/                    核验脚本与结果 JSON
├─ 赛事资料/                       四份原始赛事资料
└─ 交付物/                         当前为空，供存放最终五类材料
```

## 移动与合并

| 原位置 | 新位置或处理 |
| --- | --- |
| `华北五省赛事相关/` | 改名为 `赛事资料/`，四份原件内容不变 |
| 根目录产品方案 `.docx` | 移到 `项目文档/原始产品方案.docx` |
| `huiyan_guard/测试.jpg` | 移到 `huiyan_guard/samples/测试.jpg` |
| `tmp/audit-20260916/check_prototype.py`、`results.json` | 移到 `项目文档/核验附件/`；脚本更名为 `prototype_audit_20260916.py`，更新样图/输出路径 |
| 根目录旧 README 与 `00_项目导航.md` | 合并为短版根目录 README；开发内容并入实施方案 |
| `docs/P0.md`、`docs/architecture.md` | 有用的任务、架构原则、接口职责与验收要求并入实施方案，删除重复文件 |
| `docs/现状盘点与后续计划.md` | 事实提炼到当前进展，排期重写到实施方案，删除旧文件 |
| `huiyan_guard/慧眼安巡项目结构与数据维护说明.md` | 数据维护原则并入代码 README 和实施方案，删除旧文件 |
| `huiyan_guard/README.md` | 重写为现有代码运行说明，去掉重复规划和过时能力描述 |

## 删除清单与数量

| 类别 | 数量 | 删除依据 |
| --- | --- | --- |
| `tmp/review/` 与核验用 PDF 预览 PNG | 23 | 可从保留的原始 PDF 重新生成，不是源材料 |
| `__MACOSX/` 的 `._` 元数据 | 5 | macOS 解压附带数据，不参与工程运行 |
| 仓库内带 `(1)` 的产品方案 Word | 1 | 与保留原件 SHA-256 相同 |
| 旧导航、P0、架构、旧盘点和旧维护说明 | 5 | 必要内容已合并进新文档，避免多套计划冲突 |
| 合计 | 34 | 文件已实际删除，同时清理原空目录 |

新增实施方案、当前进展、整理记录 3 份文档，其余为移动或改写。排除 `.git` 内部文件，整理前 60 个文件，整理后 29 个文件；空间约减少 9.1MB（十进制）。根目录只保留 4 个分类目录与 1 个 README。

保留产品方案 SHA-256：`2AFBC2831D49FA8AFA35528B8D1B8C11D41C926B922C386079B813892C7B9567`。

## 文件夹整理时的验证

- 对业务源码、前端、知识清单、配置、依赖、样图、赛事原件及保留 Word 共 22 个文件进行了整理前后哈希比对，内容均未改变。
- 更新核验脚本路径后再次运行 23 个检查点，结果与整理前一致：18 项基础行为核验，5 项缺口复现。不是完整产品验收。
- Markdown 本地链接和表格列数检查通过；原代码目录未产生运行数据库或核验图片。
- `.git` 保留，未自动提交。Git 中样图旧位置显示删除、新 `samples/` 显示未跟踪，这是尚未提交的移动；两份重复/旧文档显示删除，README 显示修改。
- 批量递归删除曾被自动审批策略阻止；改为已核对的完整路径逐文件删除，随后删除确认空的目录，清理已完成。

后续总计划只改 `01_项目实施方案.md`。API、评测、设计正文等开发产物按方案在代码仓库内分别维护，最终提交文件统一进入外层 `交付物/`。

## GitHub 同步调整

2026-09-16 按用户要求同步至 `zx2881/huiyan_guard` 的 `main`。上面的目录和数量记录属于文件夹整理完成时的快照；为便于克隆仓库后阅读，后续做了以下调整：

- 三份 Markdown 项目文档从外层 `项目文档/` 移入仓库 `docs/`，未保留重复副本。
- 更新仓库 README 与本地总入口链接，仓库内的文档链接不依赖外层目录。
- 赛事四份原件、原始参赛 Word、核验附件与最终交付物保留在仓库外，本次不上传。
- 新增忽略规则，排除赛事目录、相关原件、核验附件、环境密钥、虚拟环境和本地交付物。
- 代码、前端、场景清单和移动后的样图保持原内容；本次提交包含整理造成的删除与移动。
- 仓库原有历史可能仍包含此前提交的原始产品方案；本次仅从最新版本移除，不改写团队历史。
